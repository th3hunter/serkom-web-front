import { ShowLoadingDirective } from './show-loading.directive';

describe('ShowLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
