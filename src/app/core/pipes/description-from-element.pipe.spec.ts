import { DescriptionFromElementPipe } from './description-from-element.pipe';

describe('DescriptionFromElementPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionFromElementPipe();
    expect(pipe).toBeTruthy();
  });
});
