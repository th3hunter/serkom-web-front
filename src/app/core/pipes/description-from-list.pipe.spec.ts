import { DescriptionFromListPipe } from './description-from-list.pipe';

describe('DescriptionFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
